data <- read.csv("data/AAPL.csv",
                 header=FALSE,
                 col.names=c("date", "price", "sent")
                 )
# normalize price
# find difference
# time series both
# add both together
